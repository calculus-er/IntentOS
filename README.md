# IntentOS: Intent-Based OS Layer

Local client-server hackathon project for executing natural-language intents on a host machine.

## Phase 1 status

- Base folders created: `backend/`, `frontend/`
- Local virtual environment created: `.venv/`
- Backend dependencies listed in `backend/requirements.txt`
- Environment template added: `.env.example`
- Ignore rules added: `.gitignore`
